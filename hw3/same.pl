#! /usr/bin/perl
# file: reorder.pl
use strict;
use warnings;

my $string1 = shift(@ARGV);
my $string2 = shift(@ARGV);

if(! defined($string1) or ! defined($string2)) {
	print "I need two strings, yo. 

	elsif (uc($string1) eq $string2) {
		print "same \n";
	}
	else {
		print "different \n";
	}

